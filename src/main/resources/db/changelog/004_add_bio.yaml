databaseChangeLog:
  - changeSet:
      id: 001-add-bio
      author: assistant
      changes:
        - addColumn:
            tableName: users
            columns:
              - column:
                  name: bio
                  type: varchar(256)
                  constraints:
                    nullable: true
